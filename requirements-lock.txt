# Versions serialized by the formal run manifests or pinned by the repository.
tensorflow==2.7.0
keras==2.7.0
tensorflow-estimator==2.7.0
tensorboard==2.7.0
numpy==1.21.0
scipy==1.8.1
pandas==1.4.4
matplotlib==3.5.3
scikit-learn==1.1.3
nptdms==1.10.0

# Critical TensorFlow 2.7 compatibility constraints. These prevent a fresh
# resolver from selecting newer protobuf/flatbuffers releases that satisfy
# broad historical metadata but are incompatible at runtime.
protobuf==3.19.6
flatbuffers==2.0.7
gast==0.4.0

# Versions observed in the 2026-08-13 validation environment. These packages
# were not all serialized by the historical training manifests.
seaborn==0.12.1
optuna==4.5.0
optuna-integration==4.6.0
h5py==3.14.0
Pillow==11.3.0
pytest==8.4.2

# PyMuPDF is optional and deliberately not pinned: it was absent from the
# audited validation environment. PDF inspection records that limitation.
