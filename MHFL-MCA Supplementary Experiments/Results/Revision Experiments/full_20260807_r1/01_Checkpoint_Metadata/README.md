# Checkpoint metadata

The Stage-2 and Stage-3 manifests are included to document seed, training
configuration, model specification, and checkpoint SHA-256. The approximately
30 MB `.weights.h5` files are not part of this Git package. Retrieve or rebuild
them separately and verify the manifest hash before reuse.
